name: Assemble EP01 section master
on:
  push:
    branches: ["claude/sunny-crew-ep1-assembly-8hg5om"]
    paths: ["_staging/ep01_section_urls.json"]

permissions:
  contents: write

jobs:
  assemble:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
      - name: Install ffmpeg
        run: sudo apt-get update -qq && sudo apt-get install -y -qq ffmpeg
      - name: Build section
        run: python3 scripts/ep01_build_section.py
      - name: Commit section master
        run: |
          git config user.name "section-bot"
          git config user.email "actions@github.com"
          rm -rf _staging/section_work
          git add sections/
          git commit -m "sections: EP01 section master assembled" || echo "nothing to commit"
          git push
